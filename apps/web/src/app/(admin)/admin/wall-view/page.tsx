import { WallViewClient } from './wall-view-client';

// Wall View: NO authentication required (designed for wall-mounted displays)
export default function WallViewPage() {
  return <WallViewClient />;
}
